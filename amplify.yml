version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    aseDirectory: /dist/
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*